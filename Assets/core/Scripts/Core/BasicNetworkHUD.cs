using Unity.Netcode;
using UnityEngine;

public class BasicNetworkHUD : MonoBehaviour
{
    // Debug HUD disabled. Re-enable the body of OnGUI to show
    // Start Host / Start Client / Start Server / Disconnect buttons.
    private void OnGUI()
    {
    }
}
